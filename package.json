{
  "name": "omui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "seed": "bun run scripts/seed.ts --reset",
    "test": "DEMO_DB_PATH=:memory: bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "hono": "4.12.5"
  }
}
